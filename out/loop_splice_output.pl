% out/loop_splice_output.pl
% Generated by:
%   swipl -q -s optimiser.pl \
%     -g "optimise_file('examples/loop_splice_input.pl','out/loop_splice_output.pl')" \
%     -t halt

% report/3 spliced into a single shared loop with two accumulators.
report(A,B,C) :-
    report_loop(A,[],D,[],E),
    reverse(D,B),
    reverse(E,C).
report_loop([],A,A,B,B).
report_loop([A|B],C,D,E,F) :-
    expensive(A,G),
    H=[A,G],
    other(G,I),
    J=[A,G,I],
    report_loop(B,[H|C],D,[J|E],F).

% collect/2 converted to a map accumulator loop.
collect(A,B) :-
    collect_loop(A,[],C),
    reverse(C,B).
collect_loop([],A,A).
collect_loop([A|B],C,D) :-
    f(A,E),
    collect_loop(B,[E|C],D).

% total/2 converted to a fold accumulator loop.
total(A,B) :-
    total_loop(A,0,B).
total_loop([],A,A).
total_loop([A|B],C,D) :-
    score(A,E),
    F is C+E,
    total_loop(B,F,D).
